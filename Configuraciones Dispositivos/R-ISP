------------ SHOW IP INTERFACE BRIEF (R-ISP) ------------

R-ISP#show ip interface brief

Interface                  IP-Address      Status       Protocol
Ethernet0/0                192.168.85.150  up           up
Ethernet0/1                4.0.0.13        up           up
Ethernet0/2                4.0.0.17        up           up
Ethernet0/3                4.0.0.9         up           up
Ethernet1/0                unassigned      administratively down down
Ethernet1/1                4.0.0.21        up           up
Ethernet1/2                4.0.0.25        up           up
Ethernet1/3                unassigned      administratively down down


------------ SHOW IP ROUTE (R-ISP) ------------

R-ISP#show ip route

Gateway of last resort is 192.168.85.2 to network 0.0.0.0

S*    0.0.0.0/0 [254/0] via 192.168.85.2

C        4.0.0.8/30 is directly connected, Ethernet0/3
L        4.0.0.9/32 is directly connected, Ethernet0/3

C        4.0.0.12/30 is directly connected, Ethernet0/1
L        4.0.0.13/32 is directly connected, Ethernet0/1

C        4.0.0.16/30 is directly connected, Ethernet0/2
L        4.0.0.17/32 is directly connected, Ethernet0/2

C        4.0.0.20/30 is directly connected, Ethernet1/1
L        4.0.0.21/32 is directly connected, Ethernet1/1


------------ SHOW IP OSPF NEIGHBOR (R-ISP) ------------

R-ISP#show ip ospf neighbor

No hay vecinos OSPF.


------------ SHOW IP OSPF INTERFACE BRIEF (R-ISP) ------------

R-ISP#show ip ospf interface brief

No se muestran interfaces OSPF configuradas.


------------ SHOW RUNNING-CONFIG (R-ISP) ------------

R-ISP#show running-config

hostname R-ISP

version 15.4
service timestamps debug datetime msec
service timestamps log datetime msec
no service password-encryption

no aaa new-model

ip cef
no ipv6 cef

multilink bundle-name authenticated

interface Ethernet0/0
 ip address dhcp

interface Ethernet0/1
 description Enlace_a_SANTIAGO
 ip address 4.0.0.13 255.255.255.252

interface Ethernet0/2
 description Enlace_a_ROMANA
 ip address 4.0.0.17 255.255.255.252

interface Ethernet0/3
 description Enlace_a_SANTO-DOMINGO
 ip address 4.0.0.9 255.255.255.252

interface Ethernet1/0
 no ip address
 shutdown

interface Ethernet1/1
 description Enlace_a_Barahona
 ip address 4.0.0.21 255.255.255.252

interface Ethernet1/2
 description Enlace_a_Puerto-Plata
 ip address 4.0.0.25 255.255.255.252

interface Ethernet1/3
 no ip address
 shutdown

ip forward-protocol nd

no ip http server
no ip http secure-server

