------------ SHOW IP INTERFACE BRIEF (SW1-BARAHONA) ------------

SW1-Barahona#show ip interface brief

Interface              IP-Address      Status       Protocol
Ethernet0/0            unassigned      up           up
Ethernet0/1            unassigned      up           up
Ethernet0/2            unassigned      up           up
Ethernet0/3            unassigned      up           up


------------ SHOW VLAN BRIEF (SW1-BARAHONA) ------------

SW1-Barahona#show vlan brief

VLAN  Name
1     default
401   Call-Center-Barahona
1002  fddi-default
1003  token-ring-default
1004  fddinet-default
1005  trnet-default

Puertos VLAN 401:

Et0/0 → Enlace_a_Router_Barahona
Et0/1 → PC1-Barahona

Estado:
VLAN 401 → Active


------------ SHOW INTERFACES TRUNK (SW1-BARAHONA) ------------

SW1-Barahona#show interfaces trunk

No hay interfaces trunk configuradas.


------------ SHOW SPANNING-TREE (SW1-BARAHONA) ------------

SW1-Barahona#show spanning-tree

VLAN 1
Spanning tree enabled protocol RSTP

Root ID:
Priority    32769
Address     aabb.cc00.0c00
This bridge is the root

Bridge ID:
Priority    32769
Address     aabb.cc00.0c00

Interfaces:

Et0/2 → Designated → FWD → Cost 100
Et0/3 → Designated → FWD → Cost 100


NOTA:
La salida proporcionada de Spanning Tree corresponde a VLAN 1,
no a la VLAN 401.


------------ SHOW RUNNING-CONFIG (SW1-BARAHONA) ------------

SW1-Barahona#show running-config

hostname SW1-Barahona

version 15.2
service timestamps debug datetime msec
service timestamps log datetime msec
no service password-encryption
service compress-config

no aaa new-model

ip cef
no ipv6 cef

spanning-tree mode rapid-pvst
spanning-tree extend system-id

vlan internal allocation policy ascending

interface Ethernet0/0
 description Enlace_a_Router_Barahona
 switchport access vlan 401
 switchport mode access

interface Ethernet0/1
 description PC1-Barahona
 switchport access vlan 401
 switchport mode access
 switchport nonegotiate
 switchport port-security maximum 2
 switchport port-security violation restrict
 switchport port-security mac-address sticky
 switchport port-security
 spanning-tree portfast edge
 spanning-tree bpduguard enable

interface Ethernet0/2
!

interface Ethernet0/3
!

ip forward-protocol nd

no ip http server
no ip http secure-server

line con 0
 logging synchronous

line aux 0

line vty 0 4
 login

end
